#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <sys/types.h>
int main()
{
 for(int i = 0; i < 2; i++)
 {
 fork();
 fork();
 }
 printf("Child PID: %d P-PID: %d\n", getpid(), getppid());
 return 0;
}